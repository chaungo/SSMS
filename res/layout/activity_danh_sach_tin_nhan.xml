<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="wuchou.ssms.DanhSachTinNhan" >

    <ListView
        android:id="@+id/dstn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ds_tn_btnGui"
        android:layout_alignParentTop="true"
        android:smoothScrollbar="true"
        android:stackFromBottom="true" >
    </ListView>

    <EditText
        android:id="@+id/ds_tn_edNd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/dstn"
        android:layout_alignParentBottom="true"
        android:layout_margin="10dp"
        android:layout_toLeftOf="@+id/ds_tn_btnGui"
        android:background="@drawable/ed_shape"
        android:ems="10"
        android:gravity="center"
        android:hint="Nội dung tin nhắn" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/ds_tn_btnGui"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/ic_action_send_now" />

</RelativeLayout>